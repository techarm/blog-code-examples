[package]
name = "rust-aws-credential-negative-cache"
version = "0.1.0"
edition = "2024"

[dependencies]
rusoto_credential = "0.47.0"
tokio = { version = "1", features = ["macros", "rt-multi-thread", "time"] }
async-trait = "0.1"
chrono = "0.4"
